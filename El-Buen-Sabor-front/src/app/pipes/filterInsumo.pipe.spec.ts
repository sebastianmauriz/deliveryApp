import { FilterPipeInsumo } from './filterInsumo.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeInsumo();
    expect(pipe).toBeTruthy();
  });
});